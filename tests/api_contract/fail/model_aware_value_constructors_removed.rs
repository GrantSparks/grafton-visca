use grafton_visca::types::{PanPosition, SharpnessLevel, ZoomPosition};

fn main() {
    let _ = PanPosition::new_for_model;
    let _ = SharpnessLevel::new_for_model;
    let _ = ZoomPosition::new_for_model;
}
